{% extends 'base.html.twig' %}

{% block body %}
    <h1>Account <a href="{{ account.url }}">{{ account.url }}</a></h1>
    <p>Client ID: <code>{{ account.clientId }}</code></p>

    <h2>Settings</h2>
    <a href="{{ path('specialist_index') }}">
        <button type="button" class="btn btn-outline-dark">Specialists</button>
    </a>
{% endblock %}
